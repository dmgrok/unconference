# Production Environment Configuration
# Copy this to .env.production and update with your actual values

# === CRITICAL: STRIPE PRODUCTION KEYS ===
# Get these from Stripe Dashboard (Live Mode)
STRIPE_SECRET_KEY=sk_live_your_actual_live_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_live_your_actual_live_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_production_webhook_signing_secret_here

# === DATABASE CONFIGURATION ===
# PostgreSQL (recommended for production)
DATABASE_URL="postgresql://username:password@host:5432/unconference_prod"
# OR SQLite (for smaller deployments)
# DATABASE_URL="file:./data/production.db"

# === SECURITY ===
# Generate a secure random string (32+ characters)
NUXT_SECRET_KEY=your_super_secure_32_character_production_session_key_here_make_it_random

# === APPLICATION SETTINGS ===
APP_ENV=production
NUXT_PUBLIC_BASE_URL=https://your-domain.com
NUXT_MAX_VOTES_PER_TOPIC=12
NUXT_TOP_TOPICS_COUNT=10

# === OAUTH AUTHENTICATION ===
# GitHub OAuth (Production App)
GITHUB_CLIENT_ID=your_production_github_client_id
GITHUB_CLIENT_SECRET=your_production_github_client_secret

# Google OAuth (Production App) 
GOOGLE_CLIENT_ID=your_production_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_production_google_client_secret

# LinkedIn OAuth (Optional)
LINKEDIN_CLIENT_ID=your_linkedin_client_id
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret

# === OPTIONAL: EMAIL CONFIGURATION ===
# For transactional emails (payment confirmations, etc.)
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_USER=noreply@your-domain.com
SMTP_PASS=your_smtp_password

# === OPTIONAL: MONITORING & ANALYTICS ===
# Error tracking
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Analytics
GOOGLE_ANALYTICS_ID=GA-XXXX-X

# === OPTIONAL: SOCIAL MEDIA ===
# For social sharing and metadata
TWITTER_HANDLE=@yourusername
FACEBOOK_APP_ID=your_facebook_app_id

# === PERFORMANCE & CACHING ===
# CDN URL (if using a CDN)
CDN_URL=https://cdn.your-domain.com

# Redis (for session storage and caching)
REDIS_URL=redis://localhost:6379

# === BACKUP & STORAGE ===
# S3 or similar for file uploads (if needed)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_BUCKET=your-s3-bucket-name
AWS_REGION=us-east-1
